--- module-init-tools-3.9/depmod.c	2012-02-24 13:48:03.000000000 +0100
+++ module-init-tools-3.9.orig/depmod.c	2012-02-24 13:42:11.000000000 +0100
@@ -551,6 +551,10 @@
 
 			return list;
 		}
+
+                free(old_name_strip);
+                free(old_path_strip);
+                free(new_name_strip);
 	}
 
 	/* Not in the list already. Just prepend. */
